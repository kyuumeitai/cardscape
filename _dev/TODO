Installer
Administration actions
User control panel
Card search
Card management and actions
Statistics
Recent activity
RSS feed for card changes
Export cards (JSON, XML, CSV, HTML, PDF)

-- CREATE TABLE message(
-- `cardId` INT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT ,
-- 	user INT,
--	card INT,
--	revision INT,
--	text VARCHAR(255) );
-- ) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci ;
