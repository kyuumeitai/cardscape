<?php

/**
 * List of supported card translations. This list does not affect, in any way, 
 * the available languages for the Cardscape's interface, it will only affect the 
 * list of translations that could be used while developing cards.
 * 
 * This should always return an array where the keys are the language ISO codes 
 * and the values are the language names (preferably in the language that a user 
 * can easily recognize).
 */
return array(
    'en' => 'English',
    'pt' => 'Português (Portugal)'
);